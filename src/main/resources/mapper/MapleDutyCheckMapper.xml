<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.let.service.impl.mapper.MapleDutyCheckMapper">
    <select id="searchLastMaplePoint" parameterType="String" resultType="int">
        SELECT
            maple_point
        FROM
            MAPLE_POINT_PRICE
        WHERE
            server = #{server}
        ORDER BY created_at desc
        LIMIT 1
    </select>
    <insert id="insertMaplePointHistory" parameterType="com.let.domain.MaplePointDutyCheckVO">
        INSERT INTO
            MAPLE_POINT_PRICE (maple_point, created_at,server)
        VALUES (#{maplePoint}, CURRENT_TIMESTAMP, #{server});
    </insert>
</mapper>